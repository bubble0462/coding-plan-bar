# v0.3.10

## 新增

- 供应商列表左侧新增拖拽手柄，可通过鼠标拖动调整顺序。
- 拖动时显示插入位置并支持列表边缘自动滚动。
- 拖拽手柄支持方向键上下调整，便于键盘操作。
- 保存并刷新后，托盘额度面板按照设置中的供应商顺序显示。

## 修复

- 修复 GitHub 标签已包含 `v` 时，更新页显示成 `vv0.3.9` 的问题。
- 保留拖动前的列表滚动位置和当前选中供应商。
- 重排使用现有 FLIP 动画，减少列表跳动。
